---
description: 生产级实现与交付标准
alwaysApply: true
---

# 生产级实现标准

后续所有功能、修复和文档默认按生产落地标准处理：

- 禁止玩具级或仅演示用途代码；实现必须能在真实业务环境中运行、排错和迭代。
- 禁止硬编码密钥、token、账号密码、私有 URL；敏感配置必须放入 `.env`、`config.local.js` 或部署环境变量，并提供可提交的 `*.example` 模板。
- 新增接口必须包含输入校验、错误响应、权限校验和必要审计日志；文件上传必须限制路径、大小、类型和目录逃逸。
- 变更涉及部署、配置、运行方式或故障处理时，必须同步更新文档。
- 代码应保持清晰、可维护、最小必要改动；不要引入无用依赖或无关重构。
- 提交前检查 `.gitignore` 是否覆盖本地密钥、日志、上传文件、缓存和运行态数据。
