# 复制到 /etc/default/minio，权限 600，属主 root。
# 禁止提交真实口令。

MINIO_ROOT_USER=minioadmin_CHANGE_ME
MINIO_ROOT_PASSWORD=CHANGE_ME_TO_16PLUS_CHARS

# 可选：浏览器控制台外链（内网域名）
# MINIO_BROWSER_REDIRECT_URL=https://citysafe.lab.local/minio-console/
